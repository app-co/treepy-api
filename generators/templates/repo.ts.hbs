import {  Prisma, {{pascalCase db}} } from '@prisma/client';

export interface IRepo{{pascalCase db}} {
  create(data: ): Promise<{{pascalCase db}}>;
  findById(id: string): Promise<{{pascalCase db}} | null>
  listall(): Promise<{{pascalCase db}}[]>
  delete(id: string): Promise<{{pascalCase db}}>
  update(id: string): Promise<{{pascalCase db}}>
}
