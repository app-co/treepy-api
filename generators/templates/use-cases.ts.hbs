/* eslint-disable @typescript-eslint/no-unused-vars */
import { {{pascalCase db}}, Prisma } from '@prisma/client';

interface props {
  id: string;
}


export class {{pascalCase db}}UseCases implements IRepo{{pascalCase db}} {
  constructor(
    private repo{{pascalCase db}} : IRepo{{pascalCase db}},

  ) {}

  async create(data: Prisma.{{pascalCase db}}UncheckedCreateInput): Promise<{{pascalCase db}}> {
    const create = await this.repo{{pascalCase db}}.create(data);

    return create;
  }

  async findById(id: string): Promise<{{pascalCase db}} | null> {
    const find = await this.repo{{pascalCase db}}.findById(id);

    if (!find) {
      throw new ProprertyNotFound();
    }

    return find;
  }

  async listall(): Promise<{{pascalCase db}}[]> {
    const list = this.repo{{pascalCase db}}.listall();

    return list;
  }

  async delete(id: string): Promise<{{pascalCase db}}> {
    const del = this.repo{{pascalCase db}}.delete(id);

    return del;
  }

  async update(data: IProprertyUpdate): Promise<{{pascalCase db}}> {
    const find = await this.repo{{pascalCase db}}.findById(data.id);

    if (!find) {
      throw new ProprertyNotFound();
    }
    const up = this.repo{{pascalCase db}}.update(data);

    return up;
  }

}
